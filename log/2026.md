---
layout: default
title: "Log · 2026"
permalink: /log/2026/
---

# 2026

Entries grouped by month. Each item shows **type** (theory/experiment) and tags.

{% comment %}
Build a list of 2026 posts safely (avoid using `| date` inside where_exp/if expressions).
{% endcomment %}

{% assign posts_2026 = "" | split: "" %}
{% for post in site.posts %}
  {% assign y = post.date | date: "%Y" %}
  {% if y == "2026" %}
    {% assign posts_2026 = posts_2026 | push: post %}
  {% endif %}
{% endfor %}

{% if posts_2026.size == 0 %}
  <p>No posts found for 2026 yet.</p>
{% else %}
  {% assign by_month = posts_2026 | group_by_exp: "p", "p.date | date: '%m'" %}

  {% for month in by_month %}
    <div class="section"></div>
    <h2>{{ month.name }} / 2026</h2>

    {% for post in month.items %}
      <div class="post-item">
        <a href="{{ post.url | relative_url }}">{{ post.date | date: "%m-%d" }} · {{ post.title }}</a>
        <div class="post-meta">
          {% if post.type %}<strong>{{ post.type }}</strong>{% endif %}
          {% if post.tags and post.tags.size > 0 %}
            ·
            {% for t in post.tags %}
              <a href="{{ '/tag/' | append: t | append: '/' | relative_url }}">#{{ t }}</a>{% unless forloop.last %} {% endunless %}
            {% endfor %}
          {% endif %}
        </div>
      </div>
    {% endfor %}
  {% endfor %}
{% endif %}
